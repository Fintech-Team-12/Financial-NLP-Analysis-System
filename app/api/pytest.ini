[pytest]
# app/api/ 디렉터리 기준으로 pytest rootdir 을 격리한다.
# app/api/app/ 와 상위 app/ 가 모두 `app` 이라는 이름을 쓰는 구조라,
# 여기에 자체 ini 를 두어야 rootdir=app/api 로 잡히고 PYTHONPATH=app/api 전제로
# 동작하던 기존 테스트들이 그대로 유지된다.
testpaths = tests
